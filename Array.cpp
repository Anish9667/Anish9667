#include <iostream>
using namespace std;
void Array(int arr[], int size)
{
    for (int i = 0; i < size; i++)
    {
        cout << arr[i] << " ";
    }
}
int main()
{
    int arr[5] = {2, 4, 25, 64, 7};
    int sizeofArray = 5;
  //  Array(arr, 5);
    cout<<sizeof(arr);
    cout<<"Anish";
}
